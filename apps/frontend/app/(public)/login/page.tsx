import { LoginForm } from '@/features/auth/components/login-form';

export default function LoginPage() {
  return <main className="flex min-h-[calc(100dvh-4rem)] items-center justify-center px-6"><LoginForm /></main>;
}
